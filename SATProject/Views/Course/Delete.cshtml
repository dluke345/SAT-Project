@model SATProject.Cours

@{
    ViewBag.Title = "Delete Course";
}

<h2>Delete Course</h2>

<h3>Are you sure you want to delete this?</h3>
<div id="courseWrapper">
    <div id="courseHeader">
        <h1>Course</h1><br />
    </div>
    <div id="courseRight" class="right">
        Course Name:
        @Html.DisplayFor(model => model.courseName)<br /><br />
        Department:
        @if (Model.department == null)
            {
                @:&nbsp;No Department Assigned.
            }
            else
            {
        @Html.DisplayFor(model => model.department)<br /><br />
             }
        Curriculum:
        @if (Model.curriculum == null)
            {
                @:&nbsp;No Curriculum Assigned.
            }
            else
            {
        @Html.DisplayFor(model => model.curriculum)
        }
        <br /><br />
        Course Status:
        @Html.DisplayFor(model => model.CourseStatu.CSName)<br /><br />
        Notes:<br />
        @if (Model.notes == null)
            {
                @:&nbsp;No Notes Assigned.
            }
            else
            {
        @Html.DisplayFor(model => model.notes)
        }
        <br /><br />
        Description:<br />
        @if (Model.courseDescription == null)
            {
                @:&nbsp;No Description Assigned.
            }
            else
            {
        @Html.DisplayFor(model => model.courseDescription)
        }
       
    </div>
    <div id="courseFooter">
        @using (Html.BeginForm()) {
        <input type="submit" value="Delete Course" />
            <br /><br />
        @Html.ActionLink("Back to Courses", "Index")
        }
    </div>
</div>


